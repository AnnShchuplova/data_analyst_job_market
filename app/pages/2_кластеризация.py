import streamlit as st

st.set_page_config(page_title="Кластеризация вакансий", layout="wide")

st.title("🎯 Кластеризация вакансий")
st.markdown("---")

st.info("""
**Эта страница соответствует ТЗ:**
> «Кластеризация вакансий »

**(Функциональность в разработке)**
""")

st.subheader("Параметры кластеризации")

col1, col2 = st.columns(2)

with col1:
    algorithm = st.selectbox(
        "Алгоритм кластеризации",
        ["K-Means", "DBSCAN", "Иерархическая кластеризация"]
    )
    
    n_clusters = st.slider("Количество кластеров", 2, 10, 5)

with col2:
    features = st.multiselect(
        "Признаки для кластеризации",
        ["Зарплата", "Опыт работы", "Регион", "Навыки", "Тип занятости"],
        default=["Зарплата", "Опыт работы"]
    )

if st.button("Выполнить кластеризацию"):
    with st.spinner("Выполняю кластеризацию..."):
        st.success("Кластеризация завершена!")
        
        col1, col2 = st.columns(2)
        
        with col1:
            st.subheader("Распределение по кластерам")
            # тут графики
        
        with col2:
            st.subheader("Характеристики кластеров")
            # описания